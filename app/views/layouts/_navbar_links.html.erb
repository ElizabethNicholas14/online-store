 <!-- Admin-->
 <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
      <!-- SHOP BY CATEGORY DROPDOWN -->
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Shop<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% Category.all.each do |cat| %>
              <li><%= link_to cat.name.capitalize, shop_path(cat_id: cat.id) %></li>
            <% end %>
            <li class="divider"></li>
            <li><%= link_to 'All Products', shop_path %></li>
          </ul>
        </li>
          <li><%= link_to 'About', about_path %></li>

      <!-- **ADMIN DROPDOWN** -->
      <!-- **ADMIN DROPDOWN** -->
        <% if user_signed_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Stock Room <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to 'Brands', admin_brands_path %></li>
                <li><%= link_to 'Categories', admin_categories_path %></li>
                <li><%= link_to 'Products', admin_products_path %></li>
              </ul>
            </li>
        <% end %>
       </ul>
      <!-- RIGHT NAVBAR -->
      <!-- RIGHT NAVBAR -->

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to 'Account', edit_user_registration_path %></li>
          <li><%= link_to 'Log Out', destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to 'Login', new_user_session_path %></li>
          <li><%= link_to 'Register', new_user_registration_path %></li>
        <% end %>

      </ul>
    </div><!-- /.navbar-collapse -->